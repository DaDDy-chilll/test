import { useEffect } from "react";
import Router from "./navigations/Router";

const App = () => {
  useEffect(() => {
    init();
  
  });
  
  const init = async () => {};
  return <Router />;
};

export default App;
