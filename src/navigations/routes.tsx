enum USER {
  // Auth
  LOGIN = "/login",
  FORGOT_PASSWORD = "/forgot_password",
  RESET_PASSWORD = "/reset_password",
  REGISTER = "/register",
  DASHBOARD = "/",
  CALENDAR = "/calendar",
  APPLICANTS = "/applicants",
  JOBS = "/jobs",
  MESSAGES = "/messages",
}

export default { USER };
